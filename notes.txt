extract
transform
load
            // Arrange

            // Act

            // Assert

ng-class = {md-primary: color-mode}

posts?$orderby=PublicationDate desc

http://originblogs.azurewebsites.net/
http://blogaggregator.azurewebsites.net/api/

 var blogPosts = xmlDoc.Element("rss").Element("channel").Descendants("item");
                    foreach (var post in blogPosts)
                    {
                        var newPost = new Post();
                       
                        newPost.Content = post.Element(contentNameSpace + "encoded").Value;
                        newPost.Guid = post.Element("guid").Value;
                        newPost.Link = post.Element("link").Value;
                        newPost.PublicationDate = Convert.ToDateTime(post.Element("pubDate").Value);
                        newPost.Title = post.Element("title").Value;
                        newPost.Description = post.Element("description").Value;

                        posts.Add(newPost);
}


<div layout="row">
</div>  <!-- end div layout="row" --> 
bootstrap snippet pack:
https://visualstudiogallery.msdn.microsoft.com/e82e7862-f731-4183-a27a-3a44b261bfe5
glyphfriend:  
https://visualstudiogallery.msdn.microsoft.com/5fd24afb-b3b2-4cec-9b03-1cfcec6123aa


WebJob: ReadLine
app.js: apiURL & auth
app.ctrl.js: set authenticationData.userName & authenticationData.isAuthorized
app.html: <div ng-show="authenticationData.userName.length > 0">
	  <md-button class="md-raised md-accent md-hue-1" ng-click="gotoAdmin()" ng-class="{hide: !authenticationData.isAuthorized}">
API User controller: [Authorize],  GetUsers: check for auth user
    DeleteBlog & PutBlog: [Authorize], check for auth user    
	
