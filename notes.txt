extract
transform
load
            // Arrange

            // Act

            // Assert

if (!_blogRepository.Any(b => b.BlogID == post.BlogID))

ng-class = {md-primary: color-mode}

rerun migration to add BlogType

class="md-2-line" 
flex="15"
flex="85"

       <md-list>
            <md-list-item class="md-2-line" layout = "row" layout-padding ng-repeat="post in posts">
                <div>By: XXXX XXXXXXXXXX</div>
                <div class="md-list-item-text" layout="column" layout-padding>
                    <span flex> {{post.Title}}</span>
                    <span flex>Published: {{post.PublicationDate | date: 'mediumDate'}}</span>
                    <span flex> {{post.Description}}</span>
                </div>
                <md-divider ng-if="!$last"></md-divider>
            </md-list-item>
        </md-list>

BlogType: blog.cs twice, blogmodel.cd, BlogService, Initialize in blog controller tests

                                document.getElementById("postDescription").innerHTML = post.Description;
